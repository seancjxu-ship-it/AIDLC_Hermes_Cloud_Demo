---
name: aidlc-python-development
description: Implement the smallest approved Python change from OpenSpec artifacts.
---

# Python Development / Python 开发

Read the approved requirement, spec, design, tasks, current source, and tests. Modify only the required file. Preserve public APIs unless the spec explicitly changes them. Return complete syntactically valid source. Run deterministic tests before delivery. Never include credentials.

读取已批准的需求、Spec、Design、Tasks、当前代码和测试。仅修改必要文件；除非 Spec 明确要求，否则保持公共 API 不变。输出完整且语法有效的代码，交付前执行确定性测试，禁止写入凭据。

